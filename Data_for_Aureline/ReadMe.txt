The dataset.mat file contains the heart beat sound recordings with our remote detection method. 
It contains a single matlab variable dataset, which is an array of 10x9x44100 double where the first dimension is the person ID, 
the second dimension runs over 9 different 30 sec recordings per person and the last dimension runs over signal samples at 1.5KSamples/sec.

